#ifndef VALACOMPILE_HPP
#define VALACOMPILE_HPP

#include <string>
#include <vector>

// Pass the source file and the list of flags
void handle_valacompile(const std::string& source, const std::vector<std::string>& flags);

#endif
